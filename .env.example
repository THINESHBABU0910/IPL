PORT=3000

# LLM is tried first; local simulation is the fallback on timeout/504/errors.
# Set AI_SIM_USE_LLM=false to skip LLM and use local sim only.
# AI_SIM_USE_LLM=true

# AI Gateway credentials are hardcoded in src/lib/ai/sifyClient.ts
